# ============================================================
# application.properties - Food Delivery System
# ============================================================

# ----- PostgreSQL Database Connection (Render) -----
spring.datasource.url=jdbc:postgresql://dpg-d7rmp0bbc2fs738fpmm0-a.singapore-postgres.render.com/food_delivery_weyu?sslmode=require
spring.datasource.username=partha
spring.datasource.password=pt2iDL2uI2fUH10fYu8R6TzqUShNwPTq
spring.datasource.driver-class-name=org.postgresql.Driver

# ----- Hikari CP Settings -----
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

# ----- JPA / Hibernate Settings -----
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ----- Server Port -----
server.port=${PORT:8080}
server.address=0.0.0.0
server.servlet.context-path=/

# ----- App Name -----
spring.application.name=food-delivery

# ----- CORS is handled in WebConfig.java -----

# ----- JWT Configuration -----
app.jwt.secret=${APP_JWT_SECRET:foodDeliveryJwtSecretKey2026SecureAndLongEnoughForHS256Algorithm}
app.jwt.expiration=86400000
